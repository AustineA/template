json = {
  price: post.price,
  title: post.title,
  location: "#{post.lga}, #{post.state}",
  permalink: post.permalink,
  type_of_property: post.type_of_property,
  purpose: post.purpose,
  description: post.description,
}

json[:card_thumbnail] = url_for(post.images.first.variant(combine_options: Post.medium_options)) if post.images.attached? 

json